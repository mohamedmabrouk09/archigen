package ${packageName}.adapter.out.persistence;

import ${packageName}.application.port.out.${entityName}Port;

public class ${entityName}JpaRepository implements ${entityName}Port {
    // TODO: JPA implementation
}
